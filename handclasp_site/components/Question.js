import React from 'react'
// import questions from '../static/styles/questions.css'

class Question extends React.Component {
    render() {
        return (
            <React.Fragment>
                {/* <div  className = {
                    `${questions.question_container}`
                }>
                    <h3 className = {
                        `${questions.question}`
                    }>{this.props.question}</h3>
                </div>
                <div className = "answer-container">
                    <p className = {
                        `${questions.answer}`
                    }>{this.props.answer}</p>
                </div> */}
            </React.Fragment>
        )
    }
}

export default Question